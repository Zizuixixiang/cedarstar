# .clinerules — CedarStar 项目 Token 优化指南

## 忽略的目录与文件
读取代码前，跳过以下路径，不要主动读取：
- `.git/`
- `__pycache__/`
- `.venv/` / `venv/`
- `chroma_db/`（ChromaDB 向量数据库二进制数据，无可读内容）
- `backups/`（数据库备份文件，无需读取）
- `*.pyc`
- `cedarstar.db`（SQLite 二进制文件，不要用 read_file 读取）
- `cedarstar.log`（运行日志，除非明确要查日志）
- `miniapp/node_modules/`（前端依赖，不要读取）
- `miniapp/dist/`（构建产物，不要读取）

---

## 项目结构说明
**首先读 `ARCHITECTURE.md`**，它包含：
- 完整目录结构树（每个文件的一句话说明）
- 各模块职责与边界
- 模块调用关系与数据流向
- 数据库表字段与索引（以 `ARCHITECTURE.md` 第 5 节为准，随版本演进）
- 已知的结构问题与改进建议

读完 `ARCHITECTURE.md` 后，你对项目的整体架构已有足够了解，**不需要再把所有文件都读一遍**。

---

## 按需读取策略

### 只改某个 API 接口
只需读：
- `api/router.py`（确认路由前缀）
- 对应的 `api/xxx.py`（如 `api/memory.py`）
- 如果涉及数据库操作，查 `ARCHITECTURE.md` 中的表结构说明即可，**不需要读整个 `memory/database.py`**

### 只改 Bot 消息处理逻辑
只需读：
- `bot/discord_bot.py` 或 `bot/telegram_bot.py`（二选一，两者逻辑对称）
- 如果涉及 context 构建，读 `memory/context_builder.py`

### 只改记忆系统
按需读对应子模块：
- 微批处理 → `memory/micro_batch.py`
- 日终跑批 → `memory/daily_batch.py`
- 向量检索 → `memory/vector_store.py`
- BM25 检索 → `memory/bm25_retriever.py`
- Reranker → `memory/reranker.py`

### 只改前端页面
只需读：
- `miniapp/src/router.jsx`（确认路由）
- 对应的 `miniapp/src/pages/Xxx.jsx`
- 对应的 `miniapp/src/styles/xxx.css`
- **不需要读后端代码**，API 接口格式参考 `ARCHITECTURE.md` 第 3.5 节

### 只改配置项
只需读：
- `config.py`（环境变量配置）
- `api/config.py`（运行时参数，存在 SQLite config 表）

### 数据库表结构
**直接查 `ARCHITECTURE.md` 第 5 节**，不需要读 `memory/database.py` 来了解表结构。
只有在需要修改数据库操作方法时，才读 `memory/database.py`。

---

## 关键模块速查

| 需求 | 读哪个文件 |
|------|-----------|
| 了解整体架构 | `ARCHITECTURE.md` |
| 启动流程 | `main.py` |
| 环境变量配置 | `config.py` |
| 数据库表结构 | `ARCHITECTURE.md` 第5节 |
| 数据库操作方法 | `memory/database.py` |
| LLM 调用 | `llm/llm_interface.py` |
| Context 组装 | `memory/context_builder.py` |
| Discord Bot | `bot/discord_bot.py` |
| Telegram Bot | `bot/telegram_bot.py` |
| API 路由总览 | `api/router.py` |
| 前端路由 | `miniapp/src/router.jsx` |

---

## 已知的重要约定（避免踩坑）

1. **platform 字段**：写入 `messages.platform` 时应使用 `config.py` 中 `Platform` 类的常量（`Platform.DISCORD` / `Platform.TELEGRAM`），不要直接写字符串字面量。

2. **数据库单例**：所有模块通过 `memory.database.get_database()` 获取数据库实例，不要直接 `import sqlite3` 自己操作。

3. **LLM 热更新**：Bot 中每次请求都动态 `new LLMInterface()`，不要在 `__init__` 中固化 LLM 实例，否则破坏热更新机制。

4. **dimension 枚举**：`memory_cards.dimension` 只允许 7 个值：`preferences` / `interaction_patterns` / `current_status` / `goals` / `relationships` / `key_events` / `rules`，写入前必须校验。

5. **summary_type 枚举**：`summaries.summary_type` 只允许 `chunk`（微批摘要）和 `daily`（今日小传）两个值。

6. **session_id 格式**：Discord 为 `{user_id}_{channel_id}`，Telegram 为 `telegram_{chat_id}`，两者格式不同，注意区分。

7. **API 响应格式**：所有 API 接口统一返回 `{"success": bool, "data": Any, "message": str}`，新增接口必须遵守此格式。

8. **长期记忆双写**：手动新增长期记忆时，必须同时写 SQLite `longterm_memories` 表和 ChromaDB，参考 `api/memory.py` 的 `create_longterm_memory()` 实现。

---

## 终端环境

- 当前终端是 Windows PowerShell，不支持 `&&` 连接符，多条命令请分开单独执行
- 不要用 `rm -rf`，请用 `Remove-Item -Recurse -Force`
- 不要用 `cp`，请用 `Copy-Item`
- 路径分隔符用反斜杠 `\` 或双斜杠 `\\`，不要用 Linux 风格的 `/`
- 不要用 `touch` 创建文件，请用 `New-Item`

---

## Python 环境

- 虚拟环境激活命令是 `.venv\Scripts\activate`，不是 `source .venv/bin/activate`
- pip 安装前先确认虚拟环境已激活，不要全局安装
- 不要自动运行 `pip install`，告诉我需要安装什么包，我来决定是否安装

---

## 通用行为

- 修改文件前先告诉我打算改哪些文件
- 不要一次性改动超过 3 个文件，拆分成小步骤
- `chroma_db/` 数据目录不要读取或修改

---

## 前端开发注意事项

- 开发服务器端口：`5173`（或 `5174`），Vite 已配置 `/api` 代理到 `http://localhost:8000`
- 后端 CORS 已允许 `localhost:5173` 和 `localhost:5174`
- 无 UI 组件库，纯 CSS，样式文件在 `miniapp/src/styles/` 下，每个页面对应一个 CSS 文件
- 路由使用 React Router，配置在 `miniapp/src/router.jsx`
